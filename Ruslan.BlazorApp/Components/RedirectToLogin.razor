@inject NavigationManager Navigation

@code
{
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"authentication/login?redirectUrl={Uri.EscapeDataString(Navigation.Uri)}", forceLoad: true);
    }
}